{
  "name": "expense-tracker-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "Frontend JS for the Home Assistant expense_tracker custom component.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
